[package]
name = "gtlv-py"
version = "0.1.2"
edition = "2021"
description = "极验 V3 点选/滑动求解的 Python 绑定：Rust 本地原语 + Python async 编排"
license = "AGPL-3.0-only"
repository = "https://github.com/cca2878/gtlv-py"

[lib]
name = "_native"
crate-type = ["cdylib"]

[dependencies]
# 推理核与内嵌模型。模型随该 crate 携带，本包无需任何模型文件。
# 钉到发布 tag：模型即其产物的一部分，升级须是显式动作；Cargo.lock 另记录解析出的
# 精确 commit，故可复现性不依赖 tag 本身。
gtlv-core = { git = "https://github.com/cca2878/gtlv-core", tag = "v0.1.0" }
# abi3-py310：对 CPython 稳定 ABI 编译，一个 wheel 覆盖 3.10 及以后所有版本。
# 与 requires-python = ">=3.10" 一致。
pyo3 = { version = "0.23", features = ["extension-module", "abi3-py310"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
